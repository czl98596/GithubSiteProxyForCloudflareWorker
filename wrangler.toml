name = "gh"
main = "src/index.js"
compatibility_date = "2024-04-06"

# 路由不在仓库中声明：请在 Cloudflare Dashboard 的
#   Worker → Settings → Domains & Routes 里自行添加两条路由：
#   gh.<你的域名>/*      （github.com 主站入口）
#   *-gh.<你的域名>/*    （各子域入口，如 raw-githubusercontent-com-gh.<你的域名>）


